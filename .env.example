# Copy to .env and adjust. docker-compose and the `local` Spring profile read these.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mcplatform
DB_USER=mcplatform
DB_PASSWORD=mcplatform

REDIS_HOST=localhost
REDIS_PORT=6379
# Leave empty for no Redis auth (default docker-compose setup).
REDIS_PASSWORD=
